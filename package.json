{
  "name": "rift-time-utils",
  "version": "0.2.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean-build": "rmdir /s /q dist && tsc",
    "test": "vitest",
    "test-once": "vitest run"
  },
  "devDependencies": {
    "vitest": "^0.27.2"
  }
}
